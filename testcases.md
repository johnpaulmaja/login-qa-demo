# Login Page QA Test Cases

| Title                                                      | Severity | File / URL   | Steps to Reproduce                                                                      | Expected                                       | Actual                                                   | Console / Network Errors | Suggested fix                                                      |
| ---------------------------------------------------------- | -------- | ------------ | --------------------------------------------------------------------------------------- | ---------------------------------------------- | -------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------ |
| Successful login with valid credentials                    | Low      | `index.html` | 1. Open page 2. Enter `Admin@example.com` 3. Enter `Password123` 4. Click Sign in       | Login successful message appears               | Login successful message appears                         | None                     | Accept the valid credentials and maintain success path             |
| Username comparison should be case-insensitive             | High     | `index.html` | 1. Open page 2. Enter `admin@example.com` 3. Enter `Password123` 4. Click Sign in       | Login successful message appears               | Shows "Invalid username or password."                    | None                     | Normalize username or compare case-insensitively                   |
| Password length validation boundary                        | Medium   | `index.html` | 1. Open page 2. Enter `admin@example.com` 3. Enter `Passwrd` (7 chars) 4. Click Sign in | Error: Password must be at least 8 characters. | Login successful message appears                         | None                     | Fix the password minimum length check to use `password.length < 8` |
| Password visibility toggle fails                           | Medium   | `index.html` | 1. Open page 2. Enter a password 3. Click Show                                          | Password input type changes to text            | Password input remains hidden                            | None                     | Correct the toggle logic to switch between `password` and `text`   |
| Forgot password link goes to missing page                  | Low      | `index.html` | 1. Open page 2. Click Forgot password?                                                  | Navigate to reset page or helpful information  | Navigation to `forgot-password.html` but page is missing | 404 if hosted            | Create a valid reset page or update the link to an existing route  |
| Empty username displays validation error                   | Medium   | `index.html` | 1. Open page 2. Leave username blank 3. Enter `Password123` 4. Click Sign in            | Error: Username or email cannot be blank.      | Error is displayed                                       | None                     | No fix needed if behavior matches expected                         |
| Empty password displays validation error                   | Medium   | `index.html` | 1. Open page 2. Enter `admin@example.com` 3. Leave password blank 4. Click Sign in      | Error: Password cannot be blank.               | Error is displayed                                       | None                     | No fix needed if behavior matches expected                         |
| Login button should remain disabled until inputs are valid | Medium   | `index.html` | 1. Open page 2. Leave fields empty                                                      | Sign in button disabled                        | Sign in button disabled                                  | None                     | No fix needed if behavior matches expected                         |
